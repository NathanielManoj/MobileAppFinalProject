<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/figma_dark_blue"
    tools:context=".CreateEventActivity">

    <!-- Top Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_create"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Create Event"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_gravity="center" />

    </androidx.appcompat.widget.Toolbar>

    <!-- Add Picture Area -->
    <LinearLayout
        android:id="@+id/addPictureLayout"
        android:layout_width="0dp"
        android:layout_height="150dp"
        android:background="@color/figma_light_gray"
        android:gravity="center"
        android:orientation="vertical"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar_create"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add picture"
            android:textColor="@android:color/black"
            android:textSize="18sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+"
            android:textColor="@android:color/black"
            android:textSize="48sp" />
    </LinearLayout>

    <!-- Select Date and Location -->
    <TextView
        android:id="@+id/selectDateLocationHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Date and Location"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/addPictureLayout"
        app:layout_constraintStart_toStartOf="@id/addPictureLayout" />

    <LinearLayout
        android:id="@+id/dateLocationButtons"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@color/figma_light_gray"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/selectDateLocationHeader"
        app:layout_constraintStart_toStartOf="@id/addPictureLayout"
        app:layout_constraintEnd_toEndOf="@id/addPictureLayout">

        <ImageButton
            android:id="@+id/dateButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:src="@android:drawable/ic_menu_my_calendar"
            app:tint="@android:color/black"
            tools:ignore="ContentDescription" />

        <ImageButton
            android:id="@+id/locationButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:src="@android:drawable/ic_dialog_map"
            app:tint="@android:color/black"
            tools:ignore="ContentDescription" />
    </LinearLayout>

    <!-- Add Title -->
    <TextView
        android:id="@+id/addTitleHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Title"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/dateLocationButtons"
        app:layout_constraintStart_toStartOf="@id/addPictureLayout" />

    <EditText
        android:id="@+id/titleInput"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@android:color/white"
        android:hint="Enter event title..."
        android:padding="8dp"
        android:autofillHints=""
        android:inputType="text"
        app:layout_constraintTop_toBottomOf="@id/addTitleHeader"
        app:layout_constraintStart_toStartOf="@id/addPictureLayout"
        app:layout_constraintEnd_toEndOf="@id/addPictureLayout" />

    <!-- Add Description -->
    <TextView
        android:id="@+id/addDescriptionHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Description"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/titleInput"
        app:layout_constraintStart_toStartOf="@id/addPictureLayout" />

    <EditText
        android:id="@+id/descriptionInput"
        android:layout_width="0dp"
        android:layout_height="100dp"
        android:layout_marginTop="8dp"
        android:background="@android:color/white"
        android:gravity="top"
        android:hint="Enter event details..."
        android:padding="8dp"
        android:autofillHints=""
        android:inputType="textMultiLine"
        app:layout_constraintTop_toBottomOf="@id/addDescriptionHeader"
        app:layout_constraintStart_toStartOf="@id/addPictureLayout"
        app:layout_constraintEnd_toEndOf="@id/addPictureLayout" />

    <!-- Create Button -->
    <Button
        android:id="@+id/createButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create"
        android.backgroundTint="@color/figma_orange"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:paddingHorizontal="40dp"
        android:paddingVertical="12dp"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/descriptionInput"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
